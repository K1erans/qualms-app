{
  "name": "@qualms/execution",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./testing": {
      "types": "./dist/testing.d.ts",
      "import": "./dist/testing.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts src/testing.ts --format esm --clean && tsc -p tsconfig.build.json",
    "clean": "rm -rf dist coverage",
    "format:check": "prettier --check src test *.json",
    "lint": "eslint src test",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@qualms/contracts": "workspace:*"
  },
  "devDependencies": {
    "tsup": "8.5.1"
  }
}
