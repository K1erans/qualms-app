WORKOS_CLIENT_ID=
WORKOS_API_KEY=
WORKOS_REDIRECT_URI=http://localhost:5173/callback
WORKOS_COOKIE_PASSWORD=

# Used by `vite dev` and drizzle-kit. Under `wrangler dev`, Hyperdrive uses
# `localConnectionString` from wrangler.jsonc instead.
DATABASE_URL=postgresql://user:password@localhost:5432/qualms
