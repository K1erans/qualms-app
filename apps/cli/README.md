# qualms CLI

The CLI currently exposes help, version information, and an internal NDJSON
bridge used by the Tauri desktop app. QA execution is intentionally deferred.

```sh
qualms --help
qualms bridge
```
