{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main Qualms window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
