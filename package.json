{
  "name": "qualms",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "deploy": "npm run deploy -w web",
    "lint": "oxlint && turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "db:schema": "node --env-file=.env.local --input-type=module -e \"import { execFileSync } from 'node:child_process'; const url = process.env.DATABASE_URL; if (!url) { throw new Error('DATABASE_URL is not set'); } execFileSync('psql', [url, '-f', 'db/schema.sql'], { stdio: 'inherit' });\""
  },
  "devEngines": {
    "packageManager": {
      "name": "npm",
      "version": "^11.16.0"
    }
  },
  "devDependencies": {
    "@oxlint/plugins": "^1.79.0",
    "@types/node": "^26.2.0",
    "oxlint": "^1.79.0",
    "turbo": "^2.10.11",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "daisyui": "^5.7.22",
    "tailwindcss": "^4.3.3"
  }
}
